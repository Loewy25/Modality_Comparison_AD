#!/bin/bash

#SBATCH --job-name=test         # Assign a descriptive job name
#SBATCH -N 1                    # Request 1 node
#SBATCH --mem=100G              # Request memory
#SBATCH -t 10:30:00             # Set maximum time
#SBATCH --partition=tier2_gpu   # Partition selection
#SBATCH --account=aristeidis_sotiras
#SBATCH --exclude=gpu02

#SBATCH --output=slurm-%A_%a.out
#SBATCH --error=slurm-%A_%a.err

# Load any required modules if needed
# module load <module_name>

# Change to the directory you want to inspect (optional)
cd /ceph/chpc/home/l.peiwang

# Run the 'du' command on the current directory or specify the path directly
# Using --max-depth=0 to only get the size of the current directory (no recursion)
du -sh --max-depth=0 . > directory_usage.txt

echo "Directory usage calculation completed. Results saved in directory_usage.txt"

